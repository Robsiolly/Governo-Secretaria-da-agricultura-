package com.saa.controlederegistro.ui.theme

import androidx.compose.ui.graphics.Color

// Camadas de profundidade em Grafite Escuro (Quiet Luxury - Modo Escuro Principal)
val BackgroundGrafite = Color(0xFF090A0C)     // Background profundo (nível 0)
val SurfaceGrafiteCard = Color(0xFF121417)    // Superfície de cards e listas (nível 1)
val SurfaceGrafiteElevated = Color(0xFF1B1E22)// Inputs, dropdowns e modais (nível 2)

// Ouro Velho Sóbrio (Accent único e pontual - sem vibração de laranja ou glow)
val OuroVelhoPrimario = Color(0xFFB08D57)     // Botão de ação primário ativo, marcador chave
val OuroVelhoEscuro = Color(0xFF80683F)       // Estados pressionados / ativos secundários
val ChampagneMetalico = Color(0xFFC6A96B)     // Foco elegante, contornos de destaque
val ChampagneSuave = Color(0xFFDFBA73)        // Detalhes sutis de realce de texto

// Tipografia e Legibilidade de Baixo Contraste (Apple / Nike Style)
val TextoPrincipal = Color(0xFFF3F4F6)        // Off-white confortável para leitura
val TextoSecundario = Color(0xFF8E95A1)       // Cinza médio apagado para labels secundárias e descrição
val TextoDesativado = Color(0xFF4B525E)       // Cinza escuro para itens desabilitados

// Linhas e Divisores Planos (Flat)
val BordaSutil = Color(0xFF22252C)            // Linhas de contorno finas (sem relevo/sombra)
val BordaFoco = Color(0xFF383D48)             // Contorno de foco neutro

// Cores de Identificação de Órgão
val VerdeTurismo = Color(0xFF10B981)          // Verde identificador da Sec. do Turismo (mantido sem brilho)
val VermelhoSutil = Color(0xFFEF4444)         // Alertas de erro pontuais
